name: blank

on:
  push:

jobs:
  fresh:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        apology: ["chill", "emphasize", "literature", "reverse", "unexpected"]
        rival: ["underline", "contemporary", "organ", "best"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Zs
